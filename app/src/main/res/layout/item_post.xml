<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                             xmlns:app="http://schemas.android.com/apk/res-auto"
                                             android:layout_width="match_parent"
                                             android:padding="8dp"
                                             xmlns:tools="http://schemas.android.com/tools"
                                             android:layout_margin="4dp"
                                             android:layout_height="wrap_content">

    <TextView
            android:id="@+id/tvPostTitle"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Title...."
            android:textSize="@dimen/font_18"
    />

    <ImageButton
            android:id="@+id/ibPostEdit"
            android:layout_width="@dimen/post_edit_button_size"
            android:layout_height="@dimen/post_edit_button_size"
            app:layout_constraintEnd_toStartOf="@id/ibPostDelete"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginRight="8dp"
            android:backgroundTint="@color/colorAccent"
            android:background="@drawable/ic_edit"
    />


    <ImageButton
            android:id="@+id/ibPostDelete"
            android:layout_width="@dimen/post_delete_button_size"
            android:layout_height="@dimen/post_delete_button_size"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginRight="8dp"
            android:backgroundTint="@color/colorAccent"
            android:background="@drawable/ic_delete"
    />


</android.support.constraint.ConstraintLayout>